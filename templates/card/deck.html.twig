{% extends "base.html.twig" %}

{% block title %}Card Game{% endblock %}

{% block body %}

<div class="content-header">
    <h1 class="title">Deck of Cards</span></h1>
    <div class="deck">
        {% for card in deck %} <span class="card">{{ card }}</span> {% endfor %}
    </div>
</div>

{% endblock %}